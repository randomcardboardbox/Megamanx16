#ifndef TILESEQ
#define TILESEQ

void title_sequence(void);
extern _render_text(char palette_off, int font_spr_addr, int text_ptr, int x_pos, int y_pos, char text_size, int spr_attr_address);

#endif //TILESEQ